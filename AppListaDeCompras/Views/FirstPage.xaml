<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppListaDeCompras.Views.FirstPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="FirstPage">
    <ScrollView>
        <StackLayout Padding="30">
            <Image HeightRequest="{OnPlatform Default=330, Android=290}" Source="image_cart.png" />
            <Label
                Style="{StaticResource Headline}"
                Text="Bem vindo ao &#10;Lista de Compras"
                VerticalOptions="CenterAndExpand" />
            <Label
                Style="{StaticResource SubHeadline}"
                Text="Faça suas compras de maneira organizadas e colaborativas"
                VerticalOptions="CenterAndExpand" />
            <Button Text="Acessar" VerticalOptions="EndAndExpand" />
        </StackLayout>
    </ScrollView>
</ContentPage>
